---
apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: plex
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: plex-media-server
    app.kubernetes.io/instance: plex
  ports:
    - port: 80
      targetPort: 80

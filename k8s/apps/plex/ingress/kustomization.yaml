---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingress.yaml
  # TODO(tier-demos gate): sops-create, then uncomment:
  #   plex-filebrowser-vars (flux-system) — FILES_HOST=<host under sub.<domain>>
  # - secret-vars.sops.yaml
